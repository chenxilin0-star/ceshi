/* Product List Page - Dreamy Candy Theme */

.page-product-list {
  min-height: 100vh;
  background: var(--color-bg-page);
  padding-bottom: 120rpx;
}

/* Points Bar */
.points-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: linear-gradient(135deg, #FFFFFF, #FFF8FB);
  border-radius: 28rpx;
  padding: 28rpx 32rpx;
  margin: 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 126, 179, 0.08);
  border: 1rpx solid rgba(255, 126, 179, 0.06);
}

.points-bar-left {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.points-bar-icon {
  font-size: 32rpx;
}

.points-bar .label {
  font-size: 28rpx;
  color: var(--color-text-secondary);
}

.points-bar .value {
  font-size: 44rpx;
  font-weight: 700;
  color: var(--color-accent);
  text-shadow: 0 2rpx 8rpx rgba(255, 183, 77, 0.15);
}

/* Product Grid */
.product-grid {
  display: flex;
  flex-wrap: wrap;
  padding: 0 16rpx;
  gap: 24rpx;
}

/* Product Card */
.product-card {
  width: calc(50% - 12rpx);
  background: #FFFFFF;
  border-radius: 28rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 20rpx rgba(255, 126, 179, 0.08);
  border: 1rpx solid rgba(255, 126, 179, 0.06);
  transition: all 0.2s ease;
}

.product-card:active {
  transform: scale(0.97);
}

.product-image {
  width: 100%;
  height: 280rpx;
  display: block;
}

.product-info {
  padding: 20rpx 24rpx;
}

.product-name {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text-primary);
  line-height: 1.4;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  text-overflow: ellipsis;
}

.product-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 12rpx;
}

.product-points {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--color-accent);
}

.product-remain {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
}

/* Product Card Primary Button - override global width:100% */
.product-card .btn-primary {
  width: auto;
  display: block;
  margin: 0 24rpx 24rpx;
  font-size: 24rpx;
  height: 72rpx;
  line-height: 72rpx;
  padding: 0 32rpx;
  background: linear-gradient(135deg, #FF7EB3, #D4578A);
  color: #FFFFFF;
  border-radius: 999rpx;
  text-align: center;
  font-weight: 600;
  box-shadow: 0 4rpx 16rpx rgba(255, 126, 179, 0.25);
  transition: all 0.2s ease;
}

.product-card .btn-primary:active {
  transform: scale(0.97);
  opacity: 0.9;
}
