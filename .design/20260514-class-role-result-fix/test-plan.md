# 测试计划

## 自动化测试
- `tests/test-result-presenter.test.js`
  - 历史「性格色彩测试 + 你的校园隐藏人设已生成」必须在前端展示为具体班级角色。
  - 结果页测试标题必须显示「看看你的班级隐藏角色」。
  - 不允许 summary/share/tags 继续复述生成态标题。
- `tests/submit-test-result-logic.test.js`
  - score 模式旧校园人设题遇到生成态 resultRules 时，服务端也必须返回具体班级角色。
  - 维度模式校园人设、干饭奶茶原有测试继续通过。

## 回归测试
- 抽奖逻辑测试不回归。
- WXML/JSON/JS 静态校验通过。
- `git diff --check` 无空白错误。

## 人工验收
- 点击「看看你的班级隐藏角色」答题，结果标题必须是：班级显眼包/专业摸鱼选手/低调实力派/隐藏观察者之一。
- 页面顶部不再显示「你的性格色彩测试结果是」。
- 专属结果解读不再复述「已生成」，而是解释具体角色。
